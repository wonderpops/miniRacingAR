using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class frameLEft : MonoBehaviour
{
    public enum  roads 
    { 
        begin = 0,
        forward = 1,
        right= 2,
        left = 3,
        loopup = 4,
        loopdown = 5,
    }
}
